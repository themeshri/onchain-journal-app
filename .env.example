# Copy this file to .env.local and add your API keys
# DO NOT commit .env.local to version control

# Required: Helius API for Solana transaction data
# Get your free API key at: https://dev.helius.xyz/
HELIUS_API_KEY=your_helius_api_key_here

# Optional: Solscan API for additional transaction data
# Get your API key at: https://docs.solscan.io/
SOLSCAN_API_KEY=your_solscan_api_key_here

# Optional: Other Solana RPC providers
QUICKNODE_API_KEY=
ALCHEMY_API_KEY=
SHYFT_API_KEY=

# SECURITY NOTES:
# 1. Never use NEXT_PUBLIC_ prefix for sensitive API keys
# 2. Server-side API keys should only be accessed in API routes
# 3. Always validate that API keys exist before using them
# 4. Never hardcode API keys in source code
# 5. Add .env.local to .gitignore (should already be there)